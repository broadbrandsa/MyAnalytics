// Google Ads — sync module. The ONLY place platform APIs are touched
// (CLAUDE.md hard rule #1). Windows, idempotent upserts, error mapping
// per docs/06-INTEGRATION-GOOGLE-ADS.md. Implemented in Phase 3.
export {};
