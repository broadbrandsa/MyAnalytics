// Google Analytics 4 (Data API) — sync module. The ONLY place platform APIs are touched
// (CLAUDE.md hard rule #1). Windows, idempotent upserts, error mapping
// per docs/04-INTEGRATION-GA4.md. Implemented in Phase 3.
export {};
