// Google Search Console — sync module. The ONLY place platform APIs are touched
// (CLAUDE.md hard rule #1). Windows, idempotent upserts, error mapping
// per docs/05-INTEGRATION-SEARCH-CONSOLE.md. Implemented in Phase 3.
export {};
