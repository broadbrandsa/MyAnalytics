// Meta Ads (Marketing API) — sync module. The ONLY place platform APIs are touched
// (CLAUDE.md hard rule #1). Windows, idempotent upserts, error mapping
// per docs/07-INTEGRATION-META-ADS.md. Implemented in Phase 3.
export {};
