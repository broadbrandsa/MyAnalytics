// Test stub: `server-only` throws when imported outside an RSC/server bundle.
// In the vitest node environment we alias it to this no-op so server modules
// (lib/cron, lib/supabase/*, etc.) can be unit-tested.
export {};
